export enum StackModalType {
  AddCore,
  AddCoreBud,
  Record,
  Expand,
  Preview,
  ShareView,
  AddView,
}

export enum ThrowModalType {
  Field,
  HeaderMenu,
  ViewOptions,
  RecordReference,
  Select,
  DatePicker,
  TimePicker,
  HideColumn,
}
